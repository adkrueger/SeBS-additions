# number of input and output buckets
def buckets_count():
	return (0,0)

# we shouldn't really need input for this one - it just performs "data analysis" on what is already set up in the RDS
def generate_input(data_dir, size, input_buckets, output_buckets, upload_func):
	return { }

